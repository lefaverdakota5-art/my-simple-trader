# Self-hosting note

This branch adds a Docker Compose file so the app can be hosted on your own server instead of Vercel.

Use the compose file from the repository root to run the web and API services on your own machine or VPS.
